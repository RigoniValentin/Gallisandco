auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
# Security: verify package integrity & disable lifecycle scripts by default.
# Allow only the packages you trust via `pnpm approve-builds`.
verify-store-integrity=true
side-effects-cache=true
